import type { FC } from 'react'
import { Title } from '@mantine/core'
import { BasePaper } from './base-paper'

export const WorksPaper: FC = () => (
   <BasePaper>
      <Title order={4}>Works</Title>
   </BasePaper>
)
